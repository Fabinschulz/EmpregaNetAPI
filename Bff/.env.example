# Template do .env do BFF - copie para Bff/.env.
# O docker-compose.yml do backend interpola ${BFF_PORT} a partir daqui, por isso
# o comando de subida precisa dos DOIS --env-file (backend/.env e Bff/.env).

BFF_PORT=5143

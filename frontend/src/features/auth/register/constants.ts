import type { CSSProperties } from "react";

export const REGISTER_SECTION_STYLE: CSSProperties = {
  maxWidth: 520,
  margin: "0 auto",
};

export const REGISTER_FIELDS_GRID_STYLE: CSSProperties = {
  display: "grid",
  gap: 12,
  marginTop: 14,
};
